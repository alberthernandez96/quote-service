# Local development: Postgres + Observability Stack
services:
  postgres:
    image: postgres:16-alpine
    container_name: quote-service-postgres-dev
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: quote-service
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - quote-service-network

  # Jaeger - Para visualizar traces
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: quote-service-jaeger
    restart: unless-stopped
    ports:
      - "16686:16686"  # Jaeger UI
      - "4317:4317"    # OTLP gRPC receiver
      - "4318:4318"    # OTLP HTTP receiver
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    networks:
      - quote-service-network

  # Prometheus - Para recopilar métricas
  prometheus:
    image: prom/prometheus:latest
    container_name: quote-service-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - quote-service-network

  # Grafana - Para visualizar métricas y traces (10.4 tiene Jaeger built-in; latest a veces lo tiene como plugin)
  grafana:
    image: grafana/grafana:10.4.3
    container_name: quote-service-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_FEATURE_TOGGLES_ENABLE=traceqlEditor
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    networks:
      - quote-service-network
    depends_on:
      - prometheus
      - jaeger

volumes:
  postgres_dev_data:
  prometheus_data:
  grafana_data:

networks:
  quote-service-network:
    driver: bridge
